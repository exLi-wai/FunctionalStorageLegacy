package com.xinyihl.functionalstoragelegacy.api.upgrade;

/**
 * Types of upgrade modifiers that can be applied to drawers.
 * Each storage upgrade can carry one modifier per type.
 */
public enum ModifierType {
    ITEM_STORAGE,
    FLUID_STORAGE,
    CONTROLLER_RANGE
}
